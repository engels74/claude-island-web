---































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































const steps = [
	{
		number: '01',
		title: 'Download & Install',
		description: 'Download the DMG, drag to Applications. First launch requires right-click → Open.',
		icon: 'i-lucide-download'
	},
	{
		number: '02',
		title: 'Launch the App',
		description: 'Claude Island lives in your menu bar. Click the crab icon to access the overlay.',
		icon: 'i-lucide-rocket'
	},
	{
		number: '03',
		title: 'Code with Claude',
		description:
			'Start a Claude Code session. The app auto-detects sessions and shows live updates.',
		icon: 'i-lucide-code'
	}
];
---

<section id="how-it-works" class="section relative overflow-hidden">
	<div class="container">
		<!-- Section header -->
		<div class="text-center mb-12 md:mb-20">
			<span class="badge mb-4 reveal">How It Works</span>
			<h2 class="heading-2 mb-4 reveal stagger-2">
				Up and running in
				<span class="gradient-coral">three steps</span>
			</h2>
		</div>

		<!-- Steps -->
		<div class="relative">
			<!-- Connection line (desktop) -->
			<div
				class="hidden md:block absolute top-16 left-0 right-0 h-px bg-gradient-to-r from-transparent via-white/10 to-transparent"
			>
			</div>

			<div class="grid md:grid-cols-3 gap-8 md:gap-6">
				{
					steps.map((step, index) => (
						<div class={`relative text-center reveal stagger-${index + 1}`}>
							{/* Step number */}
							<div class="relative inline-flex flex-col items-center mb-6">
								{/* Number badge */}
								<div class="w-16 h-16 rounded-2xl bg-notch-black border border-white/10 flex-center mb-4 relative z-10">
									<span class="font-pixel text-xl text-coral">{step.number}</span>
								</div>

								{/* Icon */}
								<div class="w-10 h-10 rounded-xl bg-coral/10 flex-center">
									<span class={`${step.icon} w-5 h-5 text-coral`} />
								</div>
							</div>

							{/* Content */}
							<h3 class="heading-3 mb-3">{step.title}</h3>
							<p class="body-base max-w-xs mx-auto">{step.description}</p>
						</div>
					))
				}
			</div>
		</div>
	</div>
</section>
